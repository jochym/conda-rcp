package:
  name: phonopy
  version: !!str 1.9.0.1

source:
  fn: phonopy-1.9.0.1.tar.gz
  url: http://downloads.sourceforge.net/project/phonopy/phonopy/phonopy-1.9/phonopy-1.9.0.1.tar.gz
#  md5: 6ebb277694e04f56644776d4396b3933
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  #preserve_egg_dir: True
  #entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elastic = elastic:main
    #
    # Would create an entry point called elastic that calls elastic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - spglib

  run:
    - python
    - numpy
    - matplotlib
    - spglib
    - python-yaml

test:
  # Python imports
  imports:
    - phonopy

  #commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://phonopy.sourceforge.net/
  license: BSD
  summary: 'Phonon calculation with finite displacements method.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
